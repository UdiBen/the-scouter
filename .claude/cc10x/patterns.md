<!-- CC10x Session Memory - Do not delete sections -->

## Common Gotchas
- Player names: always prefer `displayName` over `fullName` for user-facing display (displayName is optional, fallback to fullName)
- Recent searches stored in localStorage under key `'the-scouter-recent'`, max 10 entries
